@page "/payment-method" 

@using System.Net.Http.Json
@using MyApp.BlazorUI.Models
@using MyApp.BlazorUI.Services

@inject IDialogService DialogService;
@inject IAdminService PaymentService;
@inject NavigationManagerExt Navigation;


<PageTitle>Payment Method</PageTitle>

<MudText Typo="Typo.h5" Class="mt-16 mb-4">Payment Method</MudText>

<MudButton Class="mb-6" Variant="Variant.Filled" Color="Color.Success" Size="Size.Large"
           OnClick="() => OpenPaymentDialog(null)">
    Add Payment Method
</MudButton>

<MudTable Items="@payment" Hover="@hover" Filter="FilterFunc1" @bind-SelectedItem="selectedItem1">
    <ToolBarContent>
        <MudText Typo="Typo.h6">Payment Method</MudText>
        <MudSpacer />
        <MudTextField @bind-Value="searchString1" Placeholder="Search"
                      Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Search"
                      IconSize="Size.Medium" Class="mt-0" />
    </ToolBarContent>
    <HeaderContent>
        <MudTh>ID</MudTh>
        <MudTh>Name</MudTh>
        <MudTh>Logo</MudTh>
        <MudTh>Status</MudTh>
        <MudTh>Action</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd DataLabel="ID">@context.Id</MudTd>
        <MudTd DataLabel="Name">@context.Name</MudTd>
        <MudImage Class="mt-3" Src="@context.Logo" Alt="@context.Name" Width="40" Height="40" />
        <MudTd DataLabel="Status">
            @if (context.AllPayment == PaymentStatus.Active)
            {
                <MudChip T="string" Color="Color.Success" Variant="Variant.Filled" Size="Size.Small">
                    Active
                </MudChip>
            }
            else if (context.AllPayment == PaymentStatus.InActive)
            {
                <MudChip T="string" Color="Color.Error" Variant="Variant.Filled" Size="Size.Small">
                    Inactive
                </MudChip>
            }
        </MudTd>
        <MudTd DataLabel="Action">
            <MudButton Variant="Variant.Filled" Color="Color.Warning" Size="Size.Small"
                       OnClick="() => OpenPaymentDialog(context)">
                Edit
            </MudButton>
            <MudButton Variant="Variant.Filled" Color="Color.Error" Size="Size.Small"
                       Class="ml-2"
                       OnClick="() => DeletePayment(context.Id)">
                Delete
            </MudButton>
        </MudTd>
    </RowTemplate>
    <PagerContent>
        <MudTablePager />
    </PagerContent>
</MudTable>

@code {
    private IEnumerable<PaymentItem> payment = new List<PaymentItem>();
    private bool hover = true;
    private string searchString1 = "";
    private PaymentItem selectedItem1 = null;

    protected override async Task OnInitializedAsync()
    {
        await LoadPayments();
    }

    private async Task LoadPayments()
    {
        payment = await PaymentService.GetPaymentAsync();
    }

    private bool FilterFunc1(PaymentItem u) => FilterFunc(u, searchString1);

    private bool FilterFunc(PaymentItem u, string searchString)
    {
        if (string.IsNullOrWhiteSpace(searchString)) return true;
        if (u.Name.Contains(searchString, StringComparison.OrdinalIgnoreCase)) return true;
        return false;
    }

    private async Task DeletePayment(int id)
    {
        bool result = await PaymentService.DeletePaymentAsync(id);
        if (result) await LoadPayments();
    }

  private async Task OpenPaymentDialog(PaymentItem paymentItem)
    {
        var parameters = new DialogParameters();
        if (paymentItem != null)
            parameters.Add("Payment", paymentItem);

        var options = new DialogOptions
        {
            CloseButton = true,
            MaxWidth = MaxWidth.Small,
            FullWidth = true
        };

        var dialog = await DialogService.ShowAsync<PaymentDialog>("Payment Form", parameters, options);
        var result = await dialog.Result;

        if (!result.Canceled && result.Data is PaymentItem)
        {
            await LoadPayments();
        }
    }

}
